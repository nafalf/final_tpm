name: final_tpm
description: "Bantu saya ya Allah"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  crypto: ^3.0.2
  http: ^0.13.5
  intl: ^0.18.1
  flutter_map: ^4.0.0
  geolocator: ^9.0.2
  latlong2: ^0.8.1
  cupertino_icons: ^1.0.8
  url_launcher: ^6.1.7
  shared_preferences: ^2.1.0
  sensors_plus: ^3.0.4
  image_picker: ^0.8.7+4
  path_provider: ^2.0.14


dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1  # Update ke versi terbaru
  build_runner: ^2.4.8    # Update ke versi terbaru
  flutter_lints: ^5.0.0   # Hapus yang duplikat

flutter:
  uses-material-design: true